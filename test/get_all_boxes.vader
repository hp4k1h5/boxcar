~ Test get all boxes

Given (a box in a block):
  ```
  ┏━┓
  ┃ ┃
  ┗━┛
  ```

Execute (get_all boxes):
  call cursor(2,3)
  let b:block = boxcar#block#get(getcurpos(), '```')[2]
  AssertEqual ['```', '┏━┓', '┃ ┃', '┗━┛', '```'], b:block
  let b:a = boxcar#box#get_all(b:block)
  " return should be doubly nested, a list of lists of a list of y, x
  " coordinate pairs
  AssertEqual [[[1, 0], [1, 2], [3, 0], [3, 2]]], b:a

